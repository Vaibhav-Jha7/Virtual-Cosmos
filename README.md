# Virtual-Cosmos
Cosmos is a 2D virtual environment where users can move freely in a shared space and interact with each other in real time. The platform simulates real-world proximity-based communication — users automatically connect when they are close and disconnect when they move apart.
